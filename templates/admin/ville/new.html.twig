{% extends 'admin/admin_base.html.twig' %}

{% block title %}New Ville{% endblock %}

{% block body %}
  <div class="container-xxl flex-grow-1 container-p-y">
    <div class="row">
        <div class="col-xl">
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Ajouter une nouvelle Ville</h5>
                </div>
                <div class="card-body">
                   {{ include('admin/ville/_form.html.twig') }}
                </div>
            </div>
        </div>


{#        <a href="{{ path('blog_index') }}">back to list</a>#}
    </div>
</div>

    

   
{% endblock %}
