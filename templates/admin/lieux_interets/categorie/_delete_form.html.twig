<form method="post" action="{{ path('app_admin_lieux_interets_delete', {'id': categorie_lieux_interet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_lieux_interet.id) }}">
    <button class="btn">Delete</button>
</form>
