<form method="post" action="{{ path('categories_articles_delete', {'id': categories_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_article.id) }}">
    <button class="btn">Delete</button>
</form>
