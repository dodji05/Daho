{% extends 'base.html.twig' %}

{% block title %}CategoriesArticles{% endblock %}

{% block body %}
    <h1>CategoriesArticles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_article.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categories_article.nom }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categories_article.slug }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ categories_article.createdAt ? categories_article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ categories_article.updatedAt ? categories_article.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categories_articles_index') }}">back to list</a>

    <a href="{{ path('categories_articles_edit', {'id': categories_article.id}) }}">edit</a>

    {{ include('categories_articles/_delete_form.html.twig') }}
{% endblock %}
